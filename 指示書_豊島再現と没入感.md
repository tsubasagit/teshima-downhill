# 指示書: 豊島再現と没入感の向上（teshima-downhill v2）

作成: 2026-08-17 / 対象: `game.js`

## 実装状況（2026-08-17 完了）

| フェーズ | 状態 | 実装したもの |
|---|---|---|
| F1 可変勾配・大カーブ | ✅ | `COURSE` に `slope` / `bigCurve` を追加。**大カーブ3箇所**（96度・-88度・78度）。総回転486度。つなぎ目は `slopeBlendLength` で平滑化 |
| F2 遠景レイヤー | ✅ | 対岸の山なみ3層（遠いほど空色）・養殖いかだ。海面スクロールと雲の流れは実装ずみだった |
| F3 道路ディテール | ✅ | 「速度落せ」路面ペイント・カーブ標識・10%勾配標識・SLOW DOWN看板・カーブミラー・黄色ポール・電柱と電線 |
| F4 地形のうねり | ✅ | 右手の山（稜線）・草地の外ふちのうねり（道路は平らのまま）・集落クラスタ（出発／中腹は無人／港でまた増える） |
| F5 カメラ演出 | ✅ | クレスト演出（頂上で海だけが見える）・急勾配でのFOV拡張。バンクとシェイクは実装ずみだった |

## 周回制（2026-08-17 変更）

レベルを増やすのをやめ、**同じ坂を何周でも走る**方式にした。

- ゴール → 「もう一周はしる」で同じコースを再走。周をかさねると速度が少しだけ上がる（最大+45%）
- HUD は「港へ」→「2 周目」…、結果画面に さいこう記録（best）を表示
- レベル分岐がなくなったぶん、**レベル1の密度を上げる**方向に振っている

## 描画の約束ごと（さわるときの注意）

周回のたびにコースを組みなおすので、**増えつづけないこと**が最重要。

- `clearWorld()` が古い形・材質・複製テクスチャを捨てる。新しい部品を足すときも、ここに乗るようにする
- 何度も出てくる部品（黄色ポール・ガードレール支柱・電柱・家・いかだ）は `sharedGeo()` / `sharedMat()` で形を使いまわす。**毎回 new すると周回ごとにメモリが増える**
- 本数の多い同じ部品は `InstancedMesh` でまとめて1回描く（黄色ポール・ガードレール支柱が実例）
- 実測: 6周してもジオメトリ約755個で安定、描画 1.9ms/フレーム

---

## ゴール

参考写真2枚（唐櫃の坂・大カーブの頂上）の「あの感じ」を、ローポリのままで出す。
**方針: 形は単調でいい。効くのは「配置・レイヤー・動き」の3つ。**

- 写真A（前回）: 海へ下っていく直線、棚田、ガードレール
- 写真B（今回）: 坂の頂上の大カーブ。道の先が消えて海しか見えない。「速度落せ」路面ペイント、10%勾配標識、SLOW DOWN看板、カーブミラー、黄色ポール、海に養殖いかだ、対岸に何層もの山なみ

---

## フェーズ1: コースを「豊島の一本道」にする（最重要）

### 1-1. 可変勾配（セグメントごとの slope）

現在は `CONFIG.slopeRate` 固定。COURSE の各セグメントに `slope` を追加できるようにする（省略時は CONFIG値）。

```js
const COURSE = [
  { curve: 0,   length: 120, slope: 0.03 },  // 出だしはゆるく（ため）
  { curve: 85,  length: 260, slope: 0.10 },  // ★大カーブ！ 10%勾配（写真Bの標識どおり）
  { curve: -20, length: 180, slope: 0.13 },  // 一気に下る
  { curve: 30,  length: 200, slope: 0.08 },  // 棚田のS字
  { curve: -35, length: 200, slope: 0.08 },
  { curve: 0,   length: 220, slope: 0.04 },  // 海ぎわのフラット、港へ
];
```

- `buildPath()` で `seg.slope ?? CONFIG.slopeRate` を使うだけ。他は全部 path 由来なので自動で追従する
- **「ため→どん」の勾配変化が写真Bの正体**。ゆるい区間から急勾配に入る瞬間、道の先が画面から消えて海だけになる（クレスト演出、フェーズ5参照）

### 1-2. 大カーブの定義

`curve >= 60` のセグメントを「大カーブ」とみなし、開始位置の30〜60ユニット手前に標識セット（フェーズ3）を自動配置する。COURSE を走査して `bigCurves = [{startDist, dir}]` を作っておく。

---

## フェーズ2: 遠景レイヤー（幻想感はここで決まる）

写真Bの奥行きは「海 → いかだ → 対岸の山なみ（何層も、遠いほど薄い）→ 空」のレイヤー構造。

### 2-1. 対岸の山なみ（3層）

- 横に長い低い山シルエットを、coast中心の円弧上に3層置く
- 形は「つぶれたコーン」を横に5〜8個並べるだけでよい（単調でOK）
- **色が命**: 近い層ほど濃く、遠いほど空に溶ける
  - layer1（半径600）: `#7aa4bc`
  - layer2（半径900）: `#90b4c8`
  - layer3（半径1200）: `#a8c4d4`
- fog の far(520) より遠いので **`fog: false` の MeshBasicMaterial** を使い、色に「かすみ」を焼き込む（そうしないと霧で消える）

### 2-2. 海のいかだ（養殖筏）

- 濃紺の細長い箱（2×0.2×6）を 4〜6本ずつ格子に並べたグループ＝いかだ1基
- 浅瀬〜沖に 6〜10基、まばらに回転させて配置（写真Bの海面に浮かぶ黒い点列）
- これだけで「生きている瀬戸内海」になる。コスト極小

### 2-3. 海面の動き

- 海マテリアルの `texture.offset.y += dt * 0.004`（浅瀬）/ `0.002`（沖）でゆっくりスクロール
- `applyTex` で clone した texture を `SCROLLERS` 配列に登録し、animate 内で回す
- きらめきが動くだけで海の平面感が消える

### 2-4. 雲の流れ

- billboard雲に `userData.driftSpeed = 0.3〜0.8` を持たせ、animate で `position.x += drift * dt`

---

## フェーズ3: 道路の「日本の島の道」ディテール

すべてローポリ・コード生成のみ（画像不要）。大カーブ手前に自動配置。

### 3-1. 路面ペイント「速度落せ」

- Canvas 2D で白文字「速 度 落 せ」（縦書き風に1文字ずつ改行）を描き、CanvasTexture として透明平面に貼る
- 大カーブの 40 ユニット手前、路面 +0.03 の高さに置く（写真Bの再現ポイント）
- フォントは `bold 90px sans-serif`、白 `#f5f5f0`

### 3-2. 標識セット（大カーブ手前にまとめて）

| 標識 | 作り方 |
|---|---|
| 黄色ひし形カーブ標識 | 黄色平面(1×1)を45°回転+黒矢印(canvas) 、グレー支柱 |
| 10%勾配標識 | 黄色ひし形+黒文字「10%」(canvas) |
| SLOW DOWN 青看板 | 青平面(1.2×1.6)+白文字「SLOW DOWN / 減速」(canvas)、白支柱2本 |
| カーブミラー | オレンジ支柱(cylinder)+オレンジ円環+濃グレー円盤。**大カーブの外側**に置く |

### 3-3. 黄色反射ポール

- 白い支柱+黄色の頭（小さい球 or 短い円柱）
- 大カーブ区間のガードレール上に 6ユニット間隔で外側だけに並べる（写真Bのカーブ外周）

### 3-4. 電柱と電線

- 濃グレー円柱（高さ7、半径0.12）+ 横腕（box）
- 道の右side +1.5 に 40ユニット間隔
- 電線: となりの電柱間を `THREE.Line`（黒 `#2a2a2a`）で結ぶ。中点を 0.8 下げた3点カーブでたわみを出す
- **日本の田舎道の没入感は電柱が7割**。必ず入れる

---

## フェーズ4: 地形のうねり（「平面が緑」の解消）

### 4-1. 右手の山（壇山）

- 大きなつぶれた半球 or コーン（半径30〜60、高さ10〜20、緑2色）を右奥 40〜80 に 4〜6個、コースに沿って連ねる
- 稜線が見えるだけで「島の中腹を走ってる」感になる

### 4-2. 草地のうねり

- 左右の芝生リボンの**外側エッジだけ** y に `sin(d * 0.04) * 1.5 + sin(d * 0.11) * 0.7` を足す
- `buildRibbon` に「外側エッジの追加高さ関数」引数を1つ足すだけ。内側（道路側）は平らのまま＝ゲームプレイに影響なし

### 4-3. 集落クラスタ

- 家をばらまくのをやめ、**スタート直後と港の手前に「かたまり」で置く**（集落は固まるものだから）
- 家 = 白い箱+グレー切妻屋根（三角柱）。2Dの商店画像は集落内の1〜2軒だけに使う（単体で目立たせない）
- 中腹は無人（棚田と山だけ）→ 港が近づくと家が増える。**密度の変化が「島を下りてきた」物語になる**

---

## フェーズ5: カメラと体感の演出

### 5-1. クレスト演出（写真Bの瞬間）

- `slope(d+12) - slope(d) > 0.04`（この先で急に下る）のとき:
  - カメラ y を +0.8 持ち上げ、注視点を +4 遠くへ
  - 結果: 頂上で一瞬「道が消えて海だけ」になる
- lerp で滑らかに戻す

### 5-2. スピード感

- 急勾配区間（slope > 0.09）で FOV を 62→68 へ lerp（下りの加速感）
- カーブ中はカメラを進行方向へわずかにロール（`camera.rotation.z` ±0.03）

### 5-3. （任意）効果音

- 風の音ループ+ウィール走行音+レモン取得音。ファイルが必要なので後回しでよい
- 導入するなら `assets/se_*.mp3` を Web Audio で。スマホは初回タップ後に再生開始

---

## 実装順序と受け入れ基準

| 順 | フェーズ | 完了の目安 |
|---|---|---|
| 1 | F1 可変勾配+大カーブ | 頂上手前で道の先が消え、海が正面に広がる |
| 2 | F2 遠景レイヤー | 停止して見回したとき、山なみ3層といかだが見える |
| 3 | F3 道路ディテール | 大カーブ手前に「速度落せ」+標識群+ミラーが出る |
| 4 | F4 地形 | 右に山の稜線、草地にうねり、港前に集落 |
| 5 | F5 カメラ | クレストで「うわっ」となる。FOVキックが効く |

- 各フェーズ後に `startLevel(1)` → プレイ確認。**60fps を割ったらそのフェーズの物量を半分にする**
- レベル2以降はコース2倍になるため、配置ループは必ず `totalLength` 基準で書く（固定数でベタ書きしない）

## 画像素材について

- **今回のフェーズはすべてコードだけで完結する**（canvas文字・単色ジオメトリのみ。新規画像は不要）
- 任意で足すなら: 対岸の山なみ1枚絵（横長・透過PNG、うすい青のシルエット）。ただし3層コーンで十分な見込み

## CoderDojo 改造レシピへの接続

- 「速度落せ」の文字を自分の名前にする（canvas文字＝改造ポイント）
- いかだの数・山の色・電柱の間隔 → すべて CONFIG に定数として出しておくこと
